class Evaluator(words, coefs):
    if len(words) != len(coefs):
        return -1
    def zip_evaluate(words, coefs):
        len(words)/coefs
    def enumerate_evaluate(words, coefs):


#pas compris l'exo mais le but est d'apprendre à utiliser enumerate() et zip()
#forbidden functions : while